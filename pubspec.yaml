

2. pubspec.yaml

   Make sure your pubspec.yaml includes the http package dependency:

   name: random_quotes_app

   description: A basic Flutter app to display random quotes.

   publish_to: 'none'

   version: 1.0.0+1

   environment:
  
     sdk: ">=2.17.0 <3.0.0"

   dependencies:
  
     flutter:
    
          sdk: flutter
  
          http: ^0.13.5

     dev_dependencies:
  
       flutter_test:
    
              sdk: flutter
  
              flutter_lints: ^2.0.0

     flutter:
  
              uses-material-design: true
